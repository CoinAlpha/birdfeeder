handlers:
    console:
        class: logging.StreamHandler
        formatter: $STDOUT_FORMATTER
        stream: ext://sys.stdout

loggers:
    testlogger:
        level: DEBUG
        handlers: [console]
        propagate: 0
